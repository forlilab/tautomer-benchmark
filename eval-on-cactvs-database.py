#!/usr/bin/env python

from tautomerizer import Tautomerizer, get_rdkit_tautomers
from rdkit import Chem
from rdkit.Chem import Draw
from rdkit.Chem.MolStandardize import rdMolStandardize
import pandas
from PIL import ImageDraw
from PIL import ImageFont
import os
import sys

if len(sys.argv) == 2:
    smirks_fn = sys.argv[1]
else:
    smirks_fn = "smirks2.txt"
tautomerizer = Tautomerizer(smirks_fn)

font = ImageFont.truetype("FreeMono.ttf", 16)
font_small = ImageFont.truetype("FreeMono.ttf", 16)

df = pandas.read_csv("Tautomer_database_release_3a.csv")

imgsize = 300
count = 0

hits = {}
#wanted_transforms = ["PT_07_00"]
wanted_transforms = ["PT_06_00"]

success_count = 0   #
failure_count = 0   #
transform_count = 0 # stored in database of experimental data
generated_count = 0 # ideally not too many...
entries_with_none = 0
misses = set()

rdkit_success_count = 0 # generated by rdkit tautomer enumeration
rdkit_generated_count = 0

largest_Fragment = rdMolStandardize.LargestFragmentChooser()

def myeval(input_mol, target_mols, tautomer_generator_func):

    predicted_tautomers = tautomer_generator_func(input_mol)
    missed_targets = []

    target_smiles_list = []
    for mol in target_mols:
        smiles = Chem.MolToSmiles(Chem.MolFromSmiles(Chem.MolToSmiles(mol)), isomericSmiles=False)
        target_smiles_list.append(smiles)

    input_smiles = Chem.MolToSmiles(Chem.MolFromSmiles(Chem.MolToSmiles(input_mol)), isomericSmiles=False)
    target_smiles_list = [s for s in list(set(target_smiles_list)) if s != input_smiles]
    hit_mask = [False for _ in target_smiles_list]

    for t in predicted_tautomers:
        pred_smiles = Chem.MolToSmiles(Chem.RemoveHs(t), isomericSmiles=False)
        for i, target_smiles in enumerate(target_smiles_list):
            hit_mask[i] = hit_mask[i] or (smiles == pred_smiles)

    for i in range(len(hit_mask)):
        if hit_mask[i] == False:
           missed_targets.append(target_smiles_list[i])

    n_hits = sum(hit_mask)
    n_misses = len(hit_mask) - n_hits
    n_tautomers_generated = len(predicted_tautomers)
    return n_hits, n_misses, n_tautomers_generated, missed_targets

for index, row  in df.iterrows():
    #if index!=7:continue
    tautomers = [Chem.MolFromSmiles(row["SMILES_%d" % (i+1)]) for i in range(row["Size"])]
    if None in tautomers:
        print("Mol is None, index=%d, skipping" % index)
        entries_with_none += 1
        continue

    tautomers = [largest_Fragment.choose(mol) for mol in tautomers]
    
    input_mol = tautomers[0]
    target_tautomers = []
    labels = []
    transforms = []

    for i in range(row["Size"]):
        k = row["Quantitative_ratio_%d" % (i+1)]
        q = row["Qualitative_prevalence_%d" % (i+1)]
        c = row["Prevalence_Category_%d" % (i+1)]
        labels.append("%s/%s/%s" % (k, q, c))
        if i > 0:
            transform = row["Transf_1_%d" % (i+1)]
            transforms.append(transform)
            is_wanted = transform in wanted_transforms
            if is_wanted: target_tautomers.append(tautomers[i])

    if len(target_tautomers) == 0: continue

    print("Working on index=%d, input_smiles=%s" % (index, Chem.MolToSmiles(input_mol)))


    n_hits, n_misses, n_tautomers_generated, missed = myeval(input_mol, target_tautomers, tautomerizer.get_tautomers)
    success_count += n_hits
    failure_count += n_misses
    generated_count += n_tautomers_generated
    transform_count += len(target_tautomers)
    for trgt_smiles in missed:
        input_smiles = Chem.MolToSmiles(Chem.MolFromSmiles(Chem.MolToSmiles(input_mol)), isomericSmiles=False)
        misses.add((index, input_smiles, trgt_smiles))

    # same as above but for rdkit
    if False:
        try:
            n_hits, n_fail, n_tautomers_generated, _ = myeval(input_mol, target_tautomers, get_rdkit_tautomers)
            rdkit_success_count += n_hits
            rdkit_generated_count += n_tautomers_generated
        except:
            print('Oops, RDKIT failed for %s' % Chem.MolToSmiles(input_mol))

    #print('index = %d, SMILES_1: %s' % (index, Chem.MolToSmiles(tautomers[0])))

    #mols = [tautomers[i] for i in range(len(tautomers)) if selected_mask[i]]
    #texts = [labels[i] for i in range(len(tautomers)) if selected_mask[i]]
    #img = Draw.MolsToGridImage(mols, molsPerRow=sum(selected_mask),
    #        legends=texts, subImgSize=(imgsize, imgsize))
    #draw = ImageDraw.Draw(img)
    #draw.text((10, 10), row["Solvent"], (0, 0, 0), font)
    ##print(index, len(transforms))
    #for i in range(len(transforms)):
    #    draw.text((imgsize*i+int(.8*imgsize), int(0.7*imgsize)), transforms[i], (0, 0, 0), font_small)
    #    print((int(0.85*imgsize), imgsize*i+int(.8*imgsize)), transforms[i], (0, 0, 0), font_small)

    #img2, ts = tautomerizer.show_transforms(tautomers[0])
    #img.save("png-wanted/%04d-database.png" % (index))
    #img2.save("png-wanted/%04d-rdkit.png" % (index))

                #print(smiles, pred_smiles)

    #print(index, row["Size"], hit_mask, sum(hit_mask), sum(selected_mask)-1)

   # for hit in hit_mask:
   #     if not hit:
   #         print('fail, iter=%d, input=%s, taut=%s' % (index, Chem.MolToSmiles(tautomers[0], True), Chem.MolToSmiles(tautomers[hit+1], True)))

    #if index > 2000: break
    #if len(ts) == 0: continue

    #for i in range(len(transforms)):
    #    folder = transforms[i].replace(' ', '').replace('/', '--')
    #    if not os.path.isdir('png/' + folder):
    #        os.mkdir('png/' + folder) 
    #    img.save("png/%s/%04d.png" % (folder, index))
    #print(tautomers, row["Size"])
            
    #if mol.HasSubstructMatch(p1):# or mol.HasSubstructMatch(p2):
    #if mol.HasSubstructMatch(p2):
    #    print(row["Solvent"], row["pH"], row["Transf_1_2"], row["SMILES_1"], row["Quantitative_ratio_1"], row["Qualitative_prevalence_1"])
    #if (index+1) % 50 == 0: print(index)

print('index=%d' % index)
print("(%s) success: %d out of %d" % (smirks_fn, success_count, transform_count))
print("(%s) failure: %d out of %d" % (smirks_fn, failure_count, transform_count))
print("(%s) total generated tautomers: %d" % (smirks_fn, generated_count))
print("DB entries with None molecules (excluded): %d" % (entries_with_none))
print("(rdkit) success: %d out of %d" % (rdkit_success_count, transform_count))
print("(tdkit) total generated tautomers: %d" % (rdkit_generated_count))

print('Showing %d missed transforms' % len(misses))
if 1:
    os.system('rm misses/*')
    for index, i, t in misses:
        print(index, i, t)
        imol = Chem.MolFromSmiles(i)
        tmol = Chem.MolFromSmiles(t)
        img = Draw.MolsToGridImage([imol, tmol], subImgSize=(300, 300), molsPerRow=2)
        img.save('misses/%d.png' % index)
